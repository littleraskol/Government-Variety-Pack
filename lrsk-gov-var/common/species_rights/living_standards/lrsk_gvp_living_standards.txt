# Government Variety Pack
# Species Rights - Living Standards
# Author: littleraskol

# from = country
# this = species

living_standard_lrsk_forced_complementation = {
	in_breach_of = {
		{
			trigger = {
				is_gestalt = yes
			}
			key = resolution_greatergood_universal_prosperity_mandate
		}
	}

	pop_modifier = {
		pop_happiness = -0.7
	}

	potential = {
		NOR = {
			has_trait = trait_latent_psionic
			has_trait = trait_psionic
			has_trait = trait_mechanical
			has_trait = trait_machine_unit
		}
		exists = from
		from = {
			has_origin = lrsk_gvp_origin_complementation_project
			NOT = { has_tradition = tr_genetics_creation }	# Renders complementation obsolete
			has_authority = auth_hive_mind
			NOT = { has_civic = civic_hive_devouring_swarm }	# JIC
		}
	}

	allow = {
		hidden_trigger = { exists = from }
		custom_tooltip = {
			fail_text = LRSK_SPECIES_MUST_BE_COMPLEMENTING
			has_citizenship_type = {
				type = citizenship_lrsk_complementation
				country = from
			}
		}
	}

	ai_will_do = {
		factor = 100
	}
}

living_standard_lrsk_forced_complementation_psi = {
	in_breach_of = {
		{
			trigger = {
				is_gestalt = yes
			}
			key = resolution_greatergood_universal_prosperity_mandate
		}
	}

	pop_modifier = {
		pop_happiness = -0.7
	}

	potential = {
		OR = {
			has_trait = trait_latent_psionic
			has_trait = trait_psionic
		}
		NOR = {
			has_trait = trait_mechanical
			has_trait = trait_machine_unit
		}
		exists = from
		from = {
			has_origin = lrsk_gvp_origin_complementation_project
			NOT = { has_tradition = tr_genetics_creation }	# Renders complementation obsolete
			has_authority = auth_hive_mind
			NOT = { has_civic = civic_hive_devouring_swarm }	# JIC
		}
	}

	allow = {
		hidden_trigger = { exists = from }
		custom_tooltip = {
			fail_text = LRSK_SPECIES_MUST_BE_COMPLEMENTING
			has_citizenship_type = {
				type = citizenship_lrsk_complementation
				country = from
			}
		}
	}

	ai_will_do = {
		factor = 100
	}
}

